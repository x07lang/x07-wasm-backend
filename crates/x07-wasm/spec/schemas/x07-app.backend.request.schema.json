{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://x07.io/spec/x07-app.backend.request.schema.json",
  "title": "x07.app.backend.request@0.1.0",
  "description": "Backend request contract for state-doc app adapters: HTTP request envelope plus the host-retained state document.",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "request", "state"],
  "properties": {
    "schema_version": { "const": "x07.app.backend.request@0.1.0" },
    "request": { "$ref": "https://x07.io/spec/x07-http.request.envelope.schema.json" },
    "state": {
      "description": "Opaque backend state document retained by the host across requests within one app serve/test session."
    }
  }
}
