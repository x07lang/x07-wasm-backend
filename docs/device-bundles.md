# Device bundles (archived)

The device bundle format (system WebView host + pinned ABI) was archived in the
2026-06 refocus on the core deterministic execution substrate. The
`x07-wasm device` commands, the device arch registries, and the vendored
`x07-device-host` ABI snapshot have been removed from this repo.
