# Web UI (archived)

The web-ui frontend toolchain and the `std.web_ui` surface were archived in the
2026-06 refocus on the core deterministic execution substrate. The
`x07-wasm web-ui` commands, the bundled browser host, and the vendored
`x07-web-ui` inputs have been removed from this repo.
