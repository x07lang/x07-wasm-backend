# WASM iOS/Android project generation (archived)

Mobile (iOS/Android) project generation via `x07-wasm device package` was
archived in the 2026-06 refocus on the core deterministic execution substrate.
The `x07-wasm device` commands and the vendored `x07-device-host` mobile
templates have been removed from this repo.
