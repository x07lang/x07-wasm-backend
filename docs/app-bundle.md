# Full-stack app bundles (archived)

The full-stack app-bundle toolchain (web-ui frontend + `wasi:http/proxy`
backend) was archived in the 2026-06 refocus on the core deterministic
execution substrate. The `x07-wasm app` commands, the app arch registries, and
the bundled web-ui frontend have been removed from this repo.
