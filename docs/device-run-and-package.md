# Device runner: run and package (archived)

The device-host run/package toolchain was archived in the 2026-06 refocus on the
core deterministic execution substrate. The `x07-wasm device` commands and the
vendored `x07-device-host` inputs have been removed from this repo.
