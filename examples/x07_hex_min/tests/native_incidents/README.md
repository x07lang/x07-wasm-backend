`x07 Hex Min` focuses on packaged tactics/audio/haptics flows and does not ship native incident replay fixtures.

Use [`examples/x07_capture_min`](../../x07_capture_min/README.md) when you need the stricter native-incident regression fixtures for camera, location, blob, or notification paths.
